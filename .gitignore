# Ignore build generated files
build
node_modules
.lock-waf*
pebble-screenshot*.*
.*.swp
Thumbs.db
.vscode
.env
# src/pkjs/** is the babel-transpiled output of src/js (committed so Pebble
# Cloud's webpack 1 can bundle it without running babel itself). Regenerate
# with `npm run build` after editing src/js.

